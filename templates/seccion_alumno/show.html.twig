{% extends 'base.html.twig' %}

{% block title %}SeccionAlumno{% endblock %}

{% block body %}
    <h1>SeccionAlumno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seccion_alumno.id }}</td>
            </tr>
            <tr>
                <th>Fecha_creacion</th>
                <td>{{ seccion_alumno.fechaCreacion ? seccion_alumno.fechaCreacion|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ seccion_alumno.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seccion_alumno_index') }}">back to list</a>

    <a href="{{ path('app_seccion_alumno_edit', {'id': seccion_alumno.id}) }}">edit</a>

    {{ include('seccion_alumno/_delete_form.html.twig') }}
{% endblock %}
